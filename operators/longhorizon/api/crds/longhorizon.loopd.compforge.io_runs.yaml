---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: runs.longhorizon.loopd.compforge.io
spec:
  group: longhorizon.loopd.compforge.io
  names:
    kind: Run
    listKind: RunList
    plural: runs
    singular: run
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              contextMessages:
                items:
                  description: MessageReference retains visible context without copying
                    transcripts into CRDs.
                  properties:
                    conversationID:
                      maxLength: 128
                      type: string
                    messageID:
                      maxLength: 128
                      type: string
                  required:
                  - conversationID
                  - messageID
                  type: object
                maxItems: 20
                type: array
              conversation:
                properties:
                  name:
                    maxLength: 253
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    maxLength: 128
                    type: string
                required:
                - name
                - uid
                type: object
              deadlineAt:
                format: date-time
                type: string
              goal:
                maxLength: 16000
                type: string
              inputMessageID:
                maxLength: 128
                type: string
              maxRounds:
                default: 25
                format: int32
                maximum: 1000
                minimum: 1
                type: integer
              userKey:
                maxLength: 128
                type: string
              workspaceID:
                maxLength: 128
                type: string
            required:
            - conversation
            - deadlineAt
            - goal
            - inputMessageID
            - maxRounds
            - userKey
            - workspaceID
            type: object
            x-kubernetes-validations:
            - message: run inputs are immutable
              rule: self == oldSelf
          status:
            properties:
              audit:
                properties:
                  name:
                    maxLength: 253
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    maxLength: 128
                    type: string
                required:
                - name
                - uid
                type: object
              budget:
                format: int32
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consumedThrough:
                format: int32
                type: integer
              contract:
                type: string
              contractVersion:
                format: int64
                type: integer
              decision:
                description: Decision is Manager's bounded control output, not a Harness
                  checkpoint.
                properties:
                  allow_other:
                    type: boolean
                  choices:
                    items:
                      properties:
                        label:
                          type: string
                        value:
                          type: string
                      required:
                      - label
                      - value
                      type: object
                    type: array
                  contract:
                    type: string
                  next:
                    type: string
                  plan:
                    type: string
                  question:
                    type: string
                  summary:
                    type: string
                required:
                - next
                - summary
                type: object
              execution:
                properties:
                  name:
                    maxLength: 253
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    maxLength: 128
                    type: string
                required:
                - name
                - uid
                type: object
              failures:
                format: int32
                type: integer
              finalMessageID:
                type: string
              finishedAt:
                format: date-time
                type: string
              guidance:
                type: string
              humanMessageID:
                type: string
              humanReason:
                type: string
              inputMessageIDs:
                items:
                  type: string
                maxItems: 100
                type: array
              inputThrough:
                description: InputThrough is the contiguous prefix checkpointed by
                  this Run.
                type: string
              lastAudit:
                properties:
                  contractVersion:
                    format: int64
                    type: integer
                  execution:
                    properties:
                      name:
                        maxLength: 253
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        maxLength: 128
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                  messageID:
                    type: string
                  verdict:
                    properties:
                      complete:
                        type: boolean
                      evidence:
                        type: string
                      feedback:
                        type: string
                      integrity:
                        type: string
                      task_state:
                        type: string
                    required:
                    - complete
                    - evidence
                    - feedback
                    - integrity
                    - task_state
                    type: object
                required:
                - contractVersion
                - execution
                - messageID
                - verdict
                type: object
              managerMessageID:
                type: string
              phase:
                type: string
              round:
                format: int32
                type: integer
              rounds:
                items:
                  properties:
                    audit:
                      properties:
                        name:
                          maxLength: 253
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          maxLength: 128
                          type: string
                      required:
                      - name
                      - uid
                      type: object
                    decision:
                      type: string
                    execution:
                      properties:
                        name:
                          maxLength: 253
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          maxLength: 128
                          type: string
                      required:
                      - name
                      - uid
                      type: object
                    index:
                      format: int32
                      type: integer
                    messages:
                      items:
                        type: string
                      type: array
                    summary:
                      type: string
                  required:
                  - decision
                  - index
                  - summary
                  type: object
                maxItems: 50
                type: array
              summary:
                type: string
              taskState:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
