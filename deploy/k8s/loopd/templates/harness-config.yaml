apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "loopd.serverName" . }}-harnesses
  labels:
    {{- include "loopd.labels" . | nindent 4 }}
data:
  harnesses.json: {{ .Values.server.harnesses | toJson | quote }}
