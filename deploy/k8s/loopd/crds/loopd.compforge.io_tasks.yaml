---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: tasks.loopd.compforge.io
spec:
  group: loopd.compforge.io
  names:
    kind: Task
    listKind: TaskList
    plural: tasks
    singular: task
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Task is loopd's durable dispatch resource for one chat task.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              TaskSpec currently contains the minimum information needed to dispatch one
              chat task. New generic coordination fields can evolve this API additively;
              chat content and Operator-owned domain state remain outside this resource.
            properties:
              revision:
                format: int64
                minimum: 1
                type: integer
              target:
                properties:
                  key:
                    minLength: 1
                    type: string
                  kind:
                    enum:
                    - operator
                    - harness
                    type: string
                required:
                - key
                - kind
                type: object
            required:
            - revision
            - target
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
